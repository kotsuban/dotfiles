# My dotfiles (mostly terminal stuff)

## Installation

To install specific config use `stow folder-name`, for example:

- `stow nvim` - install nvim config.
- `stow macos-zsh` install zsh config for macos.
